"""Backend test suite.

A package (rather than a bare directory) so that test module names cannot
collide with modules of the same name added under other test directories in
later phases, e.g. ``tests/providers/test_registry.py`` and
``tests/pipeline/test_prompt.py``.
"""
