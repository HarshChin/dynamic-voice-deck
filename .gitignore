# secrets
.env
.env.*
!.env.example

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# coverage artefacts (.coverage is a binary SQLite file rewritten on every run)
.coverage
.coverage.*
coverage.xml
htmlcov/
coverage/

# node
node_modules/
# Voice-detector model and wasm, copied from node_modules by `npm run postinstall`.
frontend/public/vad/
dist/
*.local
# `tsc -b` incremental build state
*.tsbuildinfo

# model weights (downloaded on first run)
backend/models/
*.onnx
*.bin

# os / editors
.DS_Store
.idea/
.vscode/
